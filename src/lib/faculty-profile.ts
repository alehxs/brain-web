/**
 * Data access layer for faculty profile MDX pages.
 *
 * NOTE: This is a stub on `main`. The full implementation lives on
 * `feat/faculty-slug-pages` and will replace this file when that branch merges.
 * The sitemap and slug route depend on these exports so they are defined here
 * to keep the build green in the meantime.
 */

// eslint-disable-next-line @typescript-eslint/require-await
export async function getAllFacultyProfileSlugs(): Promise<string[]> {
  return [];
}
